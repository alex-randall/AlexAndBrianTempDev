// RedirectBanner.ts
//
// a TypeScript source code file that once compiled, implements the tiny JavaScript
// code that is run immediately on page load of every page of a SharePoint sub site
// that has the RedirectBanner applied to it via one of the Deployers.

